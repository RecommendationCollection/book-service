spring:
  cloud:
    config:
      name: book-service
      discovery:
        service-id: config
        enabled: true

---
spring:
  profiles: local

eureka:
  client:
    serviceUrl.defaultZone: http://localhost:18081/eureka/

